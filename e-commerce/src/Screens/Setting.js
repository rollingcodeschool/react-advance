import React from "react";
import { Layout, Text } from "@ui-kitten/components";

const Home = () => {
  return (
    <Layout style={{ flex: 1 }}>
      <Text>Settings</Text>
    </Layout>
  );
};

export default Home;
